// admin_cmds.pwn
// Comandos de administrador.
// Define los handlers de comandos admin (/kick, /ban, /mute, etc.), procesa la entrada de comandos y ejecuta las acciones sobre jugadores.
// Debe contener la lógica específica de cada comando y sus validaciones.

#if defined _admin_cmds_included
    #endinput
#endif
#define _admin_cmds_included